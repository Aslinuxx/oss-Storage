//V4版本增加数据校验与去重功能

程序的总体思路没有改变;
唯一有变化的是PUT请求;

程序的总体思路:api服务器在收到客户端PUT请求时候(包括PUT对象的具体数据内容和用户上传的散列值),
根据该散列值,api服务器先locate该散列值是否已存在(即是否有data服务器已经存过该内容一样的文件了);
如果已存在那么直接调用ES服务器进行版本更新即可(逻辑更新),data服务器不用再进行存储; 这是去重;

如果locate后发现data服务器没有存储该散列值文件,那么api服务器就先随机选一个data服务器, 先在该data服务器上建立一个临时的tmp文件,
然后将客户端PUT对象的具体内容写入,等传输完成,在data服务器端计算该临时文件tmp的内容的散列值是否与用户上传的散列值一致;
如果一致,则将该临时文件转为正式的存储文件; 如果不一致,则将该临时文件删除;


//这里apiServer.go中
get locate 请求均没有发生改变; 唯一有变化的即是put请求;

